# Meta Ad Analyzer: AI 기반 광고 소재 인사이트 시스템
## 제품 기획서 v1.0

---

## 📋 Executive Summary

### 제품 개요
Meta Ad Library 데이터를 기반으로 경쟁사 광고를 분석하고, AI를 활용하여 효과적인 광고 소재 제작 인사이트를 제공하는 SaaS 솔루션

### 핵심 가치 제안
- ✅ 경쟁사 광고 트렌드를 데이터 기반으로 파악
- ✅ AI가 장기 게재 광고의 성공 패턴 자동 분석
- ✅ 업종별 맞춤 소재 레이아웃 및 카피 자동 생성
- ✅ 자사 광고 성과와 비교하여 지속적 개선

### 타겟 고객
- 디지털 마케팅 에이전시 (1차 타겟: Glitzy 같은 중소형 에이전시)
- 인하우스 마케팅 팀
- 프리랜서 마케터/광고 운영자

### 수익 모델
- 월 구독 SaaS (Tier별 기능 차등)
- 업종별 패키지 (의료, 교육, 뷰티 등)
- API 사용량 기반 과금

---

## 🎯 제품 목표

### 비즈니스 목표
1. Glitzy 내부 광고 제작 효율 200% 향상
2. 클라이언트 광고 성과(CTR, CPC) 평균 30% 개선
3. 6개월 내 외부 고객 10개사 확보
4. 12개월 내 SaaS로 전환하여 MRR $5,000 달성

### 사용자 목표
1. 광고 소재 기획 시간 70% 단축
2. 데이터 기반 의사결정으로 클라이언트 설득력 향상
3. 트렌드 파악 및 벤치마킹 자동화

---

## 🏗️ 시스템 아키텍처

### 기술 스택

#### Backend
```
Language: Python 3.11+
Framework: FastAPI
Database: PostgreSQL 15
Cache: Redis 7
Queue: Celery + Redis
Storage: AWS S3 (이미지 저장)
```

#### Data Collection & Processing
```
Meta Ad Library API
Beautiful Soup / Selenium (백업용)
Pillow / OpenCV (이미지 전처리)
```

#### AI & Analysis
```
Claude 3.5 Sonnet (Vision) - 이미지 분석
GPT-4o - 카피 분석 및 생성
Claude 3.5 Sonnet - 인사이트 종합 및 추천
```

#### Frontend
```
Framework: Next.js 14 (App Router)
UI: TailwindCSS + shadcn/ui
Charts: Recharts / Chart.js
State: Zustand
```

#### Infrastructure
```
Hosting: AWS / GCP
Container: Docker + Docker Compose
CI/CD: GitHub Actions
Monitoring: Sentry + DataDog
```

---

## 📐 시스템 구조

```
┌─────────────────────────────────────────────────────────────┐
│                      Frontend (Next.js)                       │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │ 광고 탐색기   │  │ 트렌드 분석   │  │ 소재 생성기   │       │
│  └──────────────┘  └──────────────┘  └──────────────┘       │
└─────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────┐
│                    API Layer (FastAPI)                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │ Ad Collection│  │ Analysis API │  │ Generation   │       │
│  │ Endpoints    │  │              │  │ Endpoints    │       │
│  └──────────────┘  └──────────────┘  └──────────────┘       │
└─────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────┐
│                    Processing Layer                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │ Meta API     │  │ Image        │  │ AI Analysis  │       │
│  │ Collector    │  │ Processor    │  │ Engine       │       │
│  └──────────────┘  └──────────────┘  └──────────────┘       │
└─────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────┐
│                    Data Layer                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │ PostgreSQL   │  │ Redis        │  │ S3           │       │
│  │ (Metadata)   │  │ (Cache/Queue)│  │ (Images)     │       │
│  └──────────────┘  └──────────────┘  └──────────────┘       │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎨 핵심 기능 명세

### Phase 1: 광고 라이브러리 수집 + 기본 분석 대시보드

#### 1.1 광고 수집 시스템
**기능:**
- Meta Ad Library API 연동
- 업종별 키워드 기반 자동 수집
- 게재 기간, 지역, 플랫폼 필터링
- 이미지 자동 다운로드 및 저장

**데이터 스키마:**
```sql
-- ads_raw 테이블
CREATE TABLE ads_raw (
    id SERIAL PRIMARY KEY,
    ad_id VARCHAR(255) UNIQUE NOT NULL,
    page_name VARCHAR(255),
    page_id VARCHAR(255),
    ad_creative_body TEXT,
    ad_creative_link_title TEXT,
    ad_creative_link_description TEXT,
    ad_snapshot_url TEXT,
    start_date DATE,
    stop_date DATE,
    duration_days INTEGER,
    platforms TEXT[], -- ['Facebook', 'Instagram']
    currency VARCHAR(10),
    spend_lower INTEGER,
    spend_upper INTEGER,
    impressions_lower INTEGER,
    impressions_upper INTEGER,
    target_country VARCHAR(10),
    industry VARCHAR(50), -- '입시학원', '의료', '뷰티' 등
    region VARCHAR(50), -- '강남', '대치', '광주' 등
    collected_at TIMESTAMP DEFAULT NOW(),
    image_url TEXT,
    image_s3_path TEXT
);

-- ads_images 테이블
CREATE TABLE ads_images (
    id SERIAL PRIMARY KEY,
    ad_id VARCHAR(255) REFERENCES ads_raw(ad_id),
    s3_path TEXT NOT NULL,
    file_size INTEGER,
    width INTEGER,
    height INTEGER,
    format VARCHAR(10),
    created_at TIMESTAMP DEFAULT NOW()
);

-- 인덱스
CREATE INDEX idx_ads_industry ON ads_raw(industry);
CREATE INDEX idx_ads_duration ON ads_raw(duration_days);
CREATE INDEX idx_ads_region ON ads_raw(region);
CREATE INDEX idx_ads_start_date ON ads_raw(start_date);
```

**API Endpoints:**
```
POST /api/v1/ads/collect
- Body: { keywords: [], country: 'KR', industry: '입시학원', limit: 50 }
- Response: { job_id, status, estimated_time }

GET /api/v1/ads/collect/{job_id}
- Response: { status, progress, collected_count, errors }

GET /api/v1/ads/list
- Query: industry, region, min_duration, start_date, end_date, page, limit
- Response: { ads: [], total, page, pages }
```

#### 1.2 이미지 분석 엔진
**기능:**
- Claude Vision API 기반 이미지 분석
- 구성 요소, 색상, 레이아웃 자동 추출
- 감성 분석 및 강조 요소 파악

**분석 항목:**
```python
# 이미지 분석 결과 스키마
{
    "composition": {
        "has_person": true,
        "person_type": "student", # student/teacher/parent/none
        "text_ratio": 45, # 0-100%
        "has_chart": false,
        "logo_position": "bottom_right" # top_left/top_right/bottom_left/bottom_right/center/none
    },
    "colors": {
        "primary": {"color": "blue", "hex": "#1E3A8A", "rgb": [30, 58, 138]},
        "secondary": {"color": "orange", "hex": "#F59E0B", "rgb": [245, 158, 11]},
        "tertiary": {"color": "white", "hex": "#FFFFFF", "rgb": [255, 255, 255]},
        "tone": "bright", # bright/medium/dark
        "saturation": "high" # high/medium/low
    },
    "layout": {
        "type": "top_bottom_split", # top_bottom_split/left_right_split/center_focus/full_text
        "headline_position": "top",
        "cta_position": "bottom"
    },
    "emotion": {
        "atmosphere": "serious", # serious/energetic/friendly/premium
        "emphasis_elements": ["numbers", "statistics"]
    },
    "region_info": {
        "mentioned_regions": ["강남", "대치동"]
    }
}
```

**데이터 스키마:**
```sql
CREATE TABLE ads_analysis_image (
    id SERIAL PRIMARY KEY,
    ad_id VARCHAR(255) REFERENCES ads_raw(ad_id),
    has_person BOOLEAN,
    person_type VARCHAR(50),
    text_ratio INTEGER,
    has_chart BOOLEAN,
    logo_position VARCHAR(50),
    primary_color VARCHAR(7),
    secondary_color VARCHAR(7),
    tertiary_color VARCHAR(7),
    color_tone VARCHAR(20),
    saturation VARCHAR(20),
    layout_type VARCHAR(50),
    atmosphere VARCHAR(50),
    emphasis_elements TEXT[],
    mentioned_regions TEXT[],
    analysis_raw JSONB, -- 전체 분석 JSON 저장
    analyzed_at TIMESTAMP DEFAULT NOW()
);
```

#### 1.3 카피 분석 엔진
**기능:**
- GPT-4o 기반 텍스트 분석
- 카피 구조, 제안 방식, 톤앤매너 추출
- 키워드 및 감정 분석

**분석 항목:**
```python
{
    "copy_structure": {
        "headline": "목동 입시생 83%가 선택한 이유",
        "headline_length": 16,
        "body": "소규모 맞춤 관리로 평균 2등급 상승",
        "cta": "상담 신청하기",
        "core_message": "social_proof" # achievement/social_proof/free_trial/discount/management
    },
    "numbers": [
        {"value": 83, "unit": "%", "context": "선택률"},
        {"value": 2, "unit": "등급", "context": "성적 향상"}
    ],
    "regions": ["목동"],
    "offer": {
        "discount": null,
        "free_benefit": "무료 학습 진단",
        "social_proof": "83% 선택",
        "urgency": null,
        "differentiation": "소규모 맞춤 관리"
    },
    "tone": {
        "formality": "formal", # formal/informal/medium
        "emotion": "rational", # rational/emotional/balanced
        "style": "stable" # challenging/stable
    },
    "target_audience": "고등학생", # 고등학생/재수생/학부모
    "keywords": ["선택", "소규모", "맞춤", "관리", "등급", "상승"]
}
```

**데이터 스키마:**
```sql
CREATE TABLE ads_analysis_copy (
    id SERIAL PRIMARY KEY,
    ad_id VARCHAR(255) REFERENCES ads_raw(ad_id),
    headline TEXT,
    headline_length INTEGER,
    body TEXT,
    cta TEXT,
    core_message VARCHAR(50),
    numbers JSONB,
    regions TEXT[],
    discount_info VARCHAR(255),
    free_benefit VARCHAR(255),
    social_proof VARCHAR(255),
    urgency VARCHAR(255),
    differentiation TEXT,
    formality VARCHAR(20),
    emotion VARCHAR(20),
    style VARCHAR(20),
    target_audience VARCHAR(50),
    keywords TEXT[],
    analysis_raw JSONB,
    analyzed_at TIMESTAMP DEFAULT NOW()
);
```

#### 1.4 대시보드 UI

**페이지 1: 광고 갤러리**
```
┌─────────────────────────────────────────────────────────────┐
│  필터                                                         │
│  [업종: 입시학원 ▼] [지역: 전체 ▼] [게재기간: 7일+ ▼]         │
│  [플랫폼: 전체 ▼] [시즌: 3~4월 ▼]                            │
├─────────────────────────────────────────────────────────────┤
│  광고 갤러리 (Grid View)                                      │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐                        │
│  │ 광고1 │ │ 광고2 │ │ 광고3 │ │ 광고4 │                        │
│  │ 30일 │ │ 45일 │ │ 7일  │ │ 60일+│                        │
│  └──────┘ └──────┘ └──────┘ └──────┘                        │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐                        │
│  │ 광고5 │ │ 광고6 │ │ 광고7 │ │ 광고8 │                        │
│  └──────┘ └──────┘ └──────┘ └──────┘                        │
│                                                               │
│  [더보기...] [페이지네이션]                                    │
└─────────────────────────────────────────────────────────────┘

클릭 시 → 상세 분석 모달
┌─────────────────────────────────────────────────────────────┐
│  광고 상세 분석                                     [X]        │
├──────────────────┬──────────────────────────────────────────┤
│                  │  기본 정보                                │
│                  │  • 광고주: 대치 A학원                     │
│   광고 이미지     │  • 게재기간: 45일 (진행중)                │
│                  │  • 플랫폼: Facebook, Instagram            │
│                  │  • 지역: 강남/대치                        │
│                  │                                          │
│                  │  이미지 분석                              │
│                  │  • 색상: 파랑(#1E3A8A), 주황(#F59E0B)    │
│                  │  • 레이아웃: 상하분할                     │
│                  │  • 인물: 학생 (밝은 표정)                 │
│                  │  • 텍스트 비율: 45%                      │
│                  │                                          │
│                  │  카피 분석                                │
│                  │  • 헤드라인: "목동 입시생 83%가..."       │
│                  │  • 핵심 메시지: 사회적 증거               │
│                  │  • 제안: 무료 학습 진단                  │
│                  │  • 톤: 격식체, 이성적                    │
└──────────────────┴──────────────────────────────────────────┘
```

**페이지 2: 트렌드 분석**
```
┌─────────────────────────────────────────────────────────────┐
│  색상 트렌드                                                  │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ [파이차트] 주요 색상 분포                              │  │
│  │ 파랑 35% | 주황 28% | 빨강 20% | 녹색 17%              │  │
│  └───────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│  카피 트렌드                                                  │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ [워드클라우드] 자주 사용되는 키워드                     │  │
│  │ 합격 관리 최상위 소규모 맞춤 성적 향상...              │  │
│  └───────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ [막대그래프] 제안 방식별 광고 수                        │  │
│  │ 할인/프로모션 ████████ 15                              │  │
│  │ 무료 혜택     ██████████ 20                            │  │
│  │ 사회적 증거   ███████████████ 30                        │  │
│  │ 긴급성       █████ 10                                  │  │
│  └───────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│  레이아웃 트렌드                                              │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ [도넛차트] 레이아웃 분포                                │  │
│  │ 상하분할 40% | 중앙집중 30% | 좌우분할 20% | 기타 10%  │  │
│  └───────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ [막대그래프] 인물 포함 여부                             │  │
│  │ 포함 ███████████████ 35                                 │  │
│  │ 미포함 █████████ 15                                     │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

**페이지 3: 베스트 프랙티스**
```
┌─────────────────────────────────────────────────────────────┐
│  장기 게재 광고 TOP 20 (60일 이상)                            │
│  ┌─────┬──────────┬────────┬───────────────────────────┐   │
│  │ 순위│ 광고주    │ 게재일 │ 성공 요인 요약             │   │
│  ├─────┼──────────┼────────┼───────────────────────────┤   │
│  │  1  │ 대치A학원│ 87일   │ [분석 보기]                │   │
│  │  2  │ 강남B학원│ 75일   │ [분석 보기]                │   │
│  │  3  │ 목동C학원│ 68일   │ [분석 보기]                │   │
│  └─────┴──────────┴────────┴───────────────────────────┘   │
│                                                               │
│  [분석 보기] 클릭 시 → 상세 인사이트                          │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ 🎯 이 광고가 효과적인 이유                              │  │
│  │                                                         │  │
│  │ ✓ 색상: 파랑+주황 조합은 장기 게재 광고의 67%에서 사용  │  │
│  │   → 신뢰감(파랑) + 활력(주황)의 균형                   │  │
│  │                                                         │  │
│  │ ✓ 카피: "83%가 선택" 같은 구체적 숫자 + 사회적 증거    │  │
│  │   → 30일+ 게재 광고의 73%가 이 패턴 사용               │  │
│  │                                                         │  │
│  │ ✓ 레이아웃: 상단 헤드라인 + 중앙 이미지 + 하단 CTA     │  │
│  │   → 시선 흐름이 자연스럽고 명확한 구조                 │  │
│  │                                                         │  │
│  │ ✓ 인물: 밝은 표정의 학생 이미지                        │  │
│  │   → 인물 포함 광고가 미포함 대비 1.4배 장기 게재       │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

### Phase 3: AI 기반 소재 추천 시스템

#### 3.1 인사이트 도출 엔진

**기능:**
- 장기 게재 광고(30일+)의 공통 패턴 자동 추출
- 업종별, 지역별, 시즌별 성공 요인 분석
- 통계적 유의미성 검증

**분석 로직:**
```python
class InsightEngine:
    """인사이트 도출 엔진"""
    
    def extract_patterns(self, ads_data, min_duration=30):
        """
        장기 게재 광고의 패턴 추출
        
        Returns:
            {
                'color_patterns': {...},
                'copy_patterns': {...},
                'layout_patterns': {...},
                'offer_patterns': {...},
                'regional_patterns': {...}
            }
        """
        long_running_ads = self.filter_by_duration(ads_data, min_duration)
        
        patterns = {
            'color_patterns': self.analyze_color_patterns(long_running_ads),
            'copy_patterns': self.analyze_copy_patterns(long_running_ads),
            'layout_patterns': self.analyze_layout_patterns(long_running_ads),
            'offer_patterns': self.analyze_offer_patterns(long_running_ads),
            'regional_patterns': self.analyze_regional_patterns(long_running_ads)
        }
        
        return patterns
    
    def analyze_color_patterns(self, ads):
        """색상 패턴 분석"""
        color_frequency = defaultdict(int)
        color_combinations = defaultdict(int)
        
        for ad in ads:
            # 주요 색상 빈도
            primary = ad.analysis_image.primary_color
            color_frequency[primary] += 1
            
            # 색상 조합 빈도
            combo = f"{ad.analysis_image.primary_color}+{ad.analysis_image.secondary_color}"
            color_combinations[combo] += 1
        
        total = len(ads)
        
        return {
            'most_common_colors': [
                {
                    'color': color,
                    'count': count,
                    'percentage': round(count / total * 100, 1)
                }
                for color, count in sorted(color_frequency.items(), key=lambda x: x[1], reverse=True)[:5]
            ],
            'most_common_combinations': [
                {
                    'combo': combo,
                    'count': count,
                    'percentage': round(count / total * 100, 1)
                }
                for combo, count in sorted(color_combinations.items(), key=lambda x: x[1], reverse=True)[:5]
            ]
        }
    
    def analyze_copy_patterns(self, ads):
        """카피 패턴 분석"""
        keywords = defaultdict(int)
        core_messages = defaultdict(int)
        offer_types = defaultdict(int)
        
        for ad in ads:
            # 키워드 빈도
            for keyword in ad.analysis_copy.keywords:
                keywords[keyword] += 1
            
            # 핵심 메시지 유형
            core_messages[ad.analysis_copy.core_message] += 1
            
            # 제안 방식
            if ad.analysis_copy.discount_info:
                offer_types['discount'] += 1
            if ad.analysis_copy.free_benefit:
                offer_types['free_benefit'] += 1
            if ad.analysis_copy.social_proof:
                offer_types['social_proof'] += 1
            if ad.analysis_copy.urgency:
                offer_types['urgency'] += 1
        
        total = len(ads)
        
        return {
            'top_keywords': [
                {'keyword': kw, 'count': cnt, 'percentage': round(cnt / total * 100, 1)}
                for kw, cnt in sorted(keywords.items(), key=lambda x: x[1], reverse=True)[:20]
            ],
            'core_message_distribution': [
                {'type': msg, 'count': cnt, 'percentage': round(cnt / total * 100, 1)}
                for msg, cnt in sorted(core_messages.items(), key=lambda x: x[1], reverse=True)
            ],
            'offer_type_distribution': [
                {'type': offer, 'count': cnt, 'percentage': round(cnt / total * 100, 1)}
                for offer, cnt in sorted(offer_types.items(), key=lambda x: x[1], reverse=True)
            ]
        }
```

**데이터 스키마:**
```sql
CREATE TABLE insights (
    id SERIAL PRIMARY KEY,
    industry VARCHAR(50),
    region VARCHAR(50),
    season VARCHAR(20), -- 'spring', 'summer', 'fall', 'winter'
    min_duration INTEGER,
    sample_size INTEGER,
    color_patterns JSONB,
    copy_patterns JSONB,
    layout_patterns JSONB,
    offer_patterns JSONB,
    regional_patterns JSONB,
    confidence_score FLOAT, -- 0.0-1.0
    generated_at TIMESTAMP DEFAULT NOW(),
    valid_until TIMESTAMP
);
```

#### 3.2 소재 생성 엔진

**기능:**
- 사용자 입력 기반 맞춤 소재 3가지 버전 생성
- 각 버전마다 "선택 이유" 자동 생성
- 목업 이미지 렌더링

**생성 프로세스:**
```python
class CreativeGenerator:
    """소재 생성 엔진"""
    
    def __init__(self):
        self.insight_engine = InsightEngine()
        self.claude = ClaudeAPI()
        self.gpt4 = GPT4API()
    
    def generate_creatives(self, user_input):
        """
        3가지 버전의 소재 생성
        
        Args:
            user_input = {
                'business_name': '광주 A학원',
                'region': '광주',
                'target': '고3',
                'emphasis': '합격률',
                'promotion': {
                    'discount': '30%',
                    'free_benefit': '무료 상담',
                    'deadline': '2026-03-31'
                }
            }
        
        Returns:
            [version_a, version_b, version_c]
        """
        # 1. 관련 인사이트 로드
        insights = self.insight_engine.get_insights(
            industry=user_input.get('industry', '입시학원'),
            region=user_input.get('region'),
            season=self.get_current_season()
        )
        
        # 2. 3가지 전략 생성
        strategies = self.generate_strategies(user_input, insights)
        
        # 3. 각 전략에 맞는 소재 생성
        creatives = []
        for strategy in strategies:
            creative = self.create_creative(strategy, user_input, insights)
            creatives.append(creative)
        
        return creatives
    
    def generate_strategies(self, user_input, insights):
        """3가지 차별화된 전략 생성"""
        
        strategies = [
            {
                'name': 'social_proof_focused',
                'label': '사회적 증거 강조형',
                'approach': '숫자와 실적으로 신뢰 구축',
                'target_emotion': 'rational'
            },
            {
                'name': 'urgency_focused',
                'label': '긴급성 강조형',
                'approach': '마감과 선착순으로 행동 유도',
                'target_emotion': 'fomo'
            },
            {
                'name': 'premium_focused',
                'label': '프리미엄 강조형',
                'approach': '차별화된 가치와 퀄리티 부각',
                'target_emotion': 'aspiration'
            }
        ]
        
        return strategies
    
    def create_creative(self, strategy, user_input, insights):
        """개별 소재 생성"""
        
        # 1. 레이아웃 결정
        layout = self.decide_layout(strategy, insights)
        
        # 2. 색상 팔레트 결정
        colors = self.decide_colors(strategy, insights)
        
        # 3. 카피 생성 (GPT-4)
        copy = self.generate_copy(strategy, user_input, insights)
        
        # 4. "선택 이유" 생성 (Claude)
        reasoning = self.generate_reasoning(
            strategy, layout, colors, copy, insights
        )
        
        # 5. 목업 이미지 생성 (HTML/CSS 렌더링)
        mockup_html = self.render_mockup(layout, colors, copy)
        
        return {
            'strategy': strategy,
            'layout': layout,
            'colors': colors,
            'copy': copy,
            'reasoning': reasoning,
            'mockup_html': mockup_html
        }
    
    def generate_copy(self, strategy, user_input, insights):
        """GPT-4로 카피 생성"""
        
        prompt = f"""
        다음 정보를 바탕으로 {strategy['label']} 전략의 광고 카피를 작성하세요.
        
        클라이언트 정보:
        - 학원명: {user_input['business_name']}
        - 지역: {user_input['region']}
        - 타겟: {user_input['target']}
        - 강조점: {user_input['emphasis']}
        - 프로모션: {user_input.get('promotion', {})}
        
        인사이트:
        - 이 전략을 사용한 광고는 평균 {insights['strategy_stats'][strategy['name']]['avg_duration']}일 게재
        - 주요 키워드: {insights['copy_patterns']['top_keywords'][:5]}
        - 효과적인 제안 방식: {insights['offer_patterns']['most_effective']}
        
        요구사항:
        1. 헤드라인: 15-20자, 숫자 포함
        2. 본문: 30-40자, 구체적 혜택 명시
        3. CTA: 5-10자, 명확한 행동 유도
        4. 지역명 자연스럽게 포함
        
        JSON 형식으로 응답:
        {{
            "headline": "",
            "body": "",
            "cta": ""
        }}
        """
        
        response = self.gpt4.generate(prompt)
        return json.loads(response)
    
    def generate_reasoning(self, strategy, layout, colors, copy, insights):
        """Claude로 "선택 이유" 생성"""
        
        prompt = f"""
        다음 광고 소재를 왜 이렇게 디자인했는지 설명해주세요.
        
        전략: {strategy['label']}
        레이아웃: {layout}
        색상: {colors}
        카피: {copy}
        
        인사이트 데이터:
        {json.dumps(insights, ensure_ascii=False, indent=2)}
        
        다음 형식으로 설명:
        ✓ [요소]: [이유] (데이터 근거)
        
        예시:
        ✓ 파랑+주황 색상 조합: 장기 게재 광고의 67%에서 사용되며, 신뢰감과 활력을 동시에 전달
        ✓ "83% 선택" 표현: 30일+ 게재 광고의 73%가 구체적 숫자 + 사회적 증거 조합 사용
        """
        
        response = self.claude.generate(prompt)
        return response
```

**API Endpoints:**
```
POST /api/v1/creatives/generate
- Body: {
    business_name: string,
    region: string,
    target: string,
    emphasis: string,
    promotion?: {...}
  }
- Response: {
    versions: [
      {
        strategy: {...},
        layout: {...},
        colors: {...},
        copy: {...},
        reasoning: string[],
        mockup_url: string
      },
      ...
    ]
  }

GET /api/v1/creatives/{id}
- Response: 생성된 소재 상세

PUT /api/v1/creatives/{id}/edit
- Body: { copy: {...}, colors: {...} }
- Response: 수정된 소재

POST /api/v1/creatives/{id}/export
- Body: { format: 'png' | 'psd' | 'pdf' }
- Response: { download_url }
```

#### 3.3 소재 추천 UI

**소재 생성 페이지:**
```
┌─────────────────────────────────────────────────────────────┐
│  소재 생성기                                                  │
├─────────────────────────────────────────────────────────────┤
│  1️⃣ 기본 정보                                                │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ 업종:      [입시학원 ▼]                                │  │
│  │ 학원명:    [광주 A학원________________]                │  │
│  │ 지역:      [광주__________] (예: 광주, 강남, 대치)     │  │
│  │ 타겟:      [고3___________] (예: 고3, 재수생)          │  │
│  │ 강조점:    [합격률________] (예: 합격률, 소규모 관리)  │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                               │
│  2️⃣ 프로모션 정보 (선택)                                     │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ ☐ 할인율:      [30_____] %                             │  │
│  │ ☐ 무료 혜택:   [무료 상담__________________]           │  │
│  │ ☐ 마감 기한:   [2026-03-31_____]                       │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                               │
│  [AI 소재 생성하기]                                           │
└─────────────────────────────────────────────────────────────┘

생성 중...
┌─────────────────────────────────────────────────────────────┐
│  🤖 AI가 소재를 생성하고 있습니다...                          │
│                                                               │
│  ✅ 관련 광고 50개 분석 완료                                  │
│  ✅ 색상 트렌드 분석 완료                                     │
│  ⏳ 카피 생성 중... (2/3)                                     │
│  ⏳ 목업 렌더링 중...                                         │
│                                                               │
│  [진행률: ████████░░ 75%]                                     │
└─────────────────────────────────────────────────────────────┘

결과 화면
┌─────────────────────────────────────────────────────────────┐
│  생성된 소재 (3가지 버전)                                     │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────────────┐ ┌──────────────────┐ ┌───────────────┐│
│  │ Version A        │ │ Version B        │ │ Version C     ││
│  │ 사회적 증거 강조 │ │ 긴급성 강조      │ │ 프리미엄 강조 ││
│  ├──────────────────┤ ├──────────────────┤ ├───────────────┤│
│  │ [목업 이미지]    │ │ [목업 이미지]    │ │ [목업 이미지] ││
│  │                  │ │                  │ │               ││
│  │ 헤드라인:        │ │ 헤드라인:        │ │ 헤드라인:     ││
│  │ "광주 입시생     │ │ "2025 수능       │ │ "광주 최고급  ││
│  │  82%가 선택"    │ │  D-247일"        │ │  맞춤 관리"   ││
│  │                  │ │                  │ │               ││
│  │ 본문:            │ │ 본문:            │ │ 본문:         ││
│  │ "소규모 밀착     │ │ "선착순 30명     │ │ "1:1 진단부터 ││
│  │  관리로 평균     │ │  한정 3월 정규반 │ │  SKY 합격까지"││
│  │  2등급 상승"     │ │  마감 임박"      │ │               ││
│  │                  │ │                  │ │               ││
│  │ [왜 이렇게?]     │ │ [왜 이렇게?]     │ │ [왜 이렇게?]  ││
│  │ [이 버전 선택]   │ │ [이 버전 선택]   │ │ [이 버전 선택]││
│  └──────────────────┘ └──────────────────┘ └───────────────┘│
└─────────────────────────────────────────────────────────────┘

[왜 이렇게?] 클릭 시
┌─────────────────────────────────────────────────────────────┐
│  🎯 Version A - 이렇게 추천한 이유                            │
│                                                               │
│  ✓ 색상: 파랑(#1E3A8A) + 주황(#F59E0B)                       │
│    → 30일+ 게재 광고의 67%에서 사용하는 조합                  │
│    → 신뢰감(파랑)과 활력(주황)을 동시에 전달                  │
│                                                               │
│  ✓ 카피: "82% 선택" + 구체적 성과(2등급 상승)                │
│    → 장기 게재 광고의 73%가 '숫자+사회적 증거' 조합 사용      │
│    → 광주 지역 광고의 82%가 '소규모 관리' 강조               │
│                                                               │
│  ✓ 레이아웃: 상단 헤드라인 + 중앙 학생 이미지 + 하단 CTA     │
│    → 시선 흐름이 자연스러운 F자형 패턴                        │
│    → 인물 포함 광고가 미포함 대비 1.4배 더 장기 게재됨        │
│                                                               │
│  ✓ 지역 정보: "광주" 명시로 지역 밀착 이미지 구축             │
│    → 광주 광고의 90%가 지역명을 헤드라인에 포함               │
│                                                               │
│  📊 예상 성과:                                                │
│    이 패턴을 사용한 광고의 평균 게재 기간: 42일               │
│    업종 평균 대비 성과: +35%                                  │
└─────────────────────────────────────────────────────────────┘

[이 버전 선택] 클릭 시
┌─────────────────────────────────────────────────────────────┐
│  소재 편집                                                    │
├──────────────────┬──────────────────────────────────────────┤
│                  │  카피 수정                                │
│   [목업 미리보기] │  헤드라인: [광주 입시생 82%가 선택_____]  │
│                  │  본문:     [소규모 밀착 관리로 평균____]  │
│   실시간 반영     │  CTA:      [상담 신청하기______________]  │
│                  │                                          │
│                  │  색상 조정                                │
│                  │  주 색상: [#1E3A8A] 🎨                   │
│                  │  보조색:  [#F59E0B] 🎨                   │
│                  │  배경:    [#FFFFFF] 🎨                   │
│                  │                                          │
│                  │  레이아웃 변경                            │
│                  │  ( ) 상하분할  (●) 좌우분할              │
│                  │  ( ) 중앙집중  ( ) 전면텍스트             │
│                  │                                          │
│                  │  [저장] [다운로드]                        │
└──────────────────┴──────────────────────────────────────────┘

다운로드 옵션
┌─────────────────────────────────────────────────────────────┐
│  파일 형식 선택                                               │
│  ☐ PNG (1080x1080, 웹용)                                     │
│  ☐ PNG (1200x628, Facebook 피드)                             │
│  ☐ PNG (1080x1920, Instagram 스토리)                         │
│  ☐ PSD (Photoshop 편집용)                                    │
│  ☐ PDF (인쇄용)                                              │
│                                                               │
│  [선택한 형식 모두 다운로드]                                  │
└─────────────────────────────────────────────────────────────┘
```

---

### Phase 2: 자사 광고 성과 연동 및 비교 분석

#### 2.1 Meta Marketing API 연동

**기능:**
- 자사 광고 계정 연동
- 실시간 성과 데이터 수집 (노출, 클릭, CPC, CTR, 전환 등)
- 캠페인/광고세트/광고 단위 데이터 수집

**데이터 스키마:**
```sql
CREATE TABLE own_campaigns (
    id SERIAL PRIMARY KEY,
    ad_account_id VARCHAR(255),
    campaign_id VARCHAR(255) UNIQUE,
    campaign_name TEXT,
    status VARCHAR(50),
    objective VARCHAR(50),
    daily_budget INTEGER,
    lifetime_budget INTEGER,
    start_time TIMESTAMP,
    stop_time TIMESTAMP,
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);

CREATE TABLE own_ads (
    id SERIAL PRIMARY KEY,
    ad_account_id VARCHAR(255),
    campaign_id VARCHAR(255) REFERENCES own_campaigns(campaign_id),
    ad_id VARCHAR(255) UNIQUE,
    ad_name TEXT,
    status VARCHAR(50),
    creative_id VARCHAR(255),
    image_url TEXT,
    image_s3_path TEXT,
    headline TEXT,
    body TEXT,
    cta TEXT,
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);

CREATE TABLE own_ad_insights (
    id SERIAL PRIMARY KEY,
    ad_id VARCHAR(255) REFERENCES own_ads(ad_id),
    date DATE,
    impressions INTEGER,
    clicks INTEGER,
    spend DECIMAL(10,2),
    reach INTEGER,
    frequency DECIMAL(10,2),
    ctr DECIMAL(10,4),
    cpc DECIMAL(10,2),
    cpm DECIMAL(10,2),
    conversions INTEGER,
    conversion_value DECIMAL(10,2),
    cost_per_conversion DECIMAL(10,2),
    roas DECIMAL(10,2),
    created_at TIMESTAMP DEFAULT NOW(),
    UNIQUE(ad_id, date)
);
```

#### 2.2 비교 분석 대시보드

**페이지: 성과 비교**
```
┌─────────────────────────────────────────────────────────────┐
│  자사 광고 vs 경쟁사 벤치마크                                 │
├─────────────────────────────────────────────────────────────┤
│  기간: [2026-02-01] ~ [2026-02-28]                           │
│                                                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ 주요 지표                                              │  │
│  │ ┌──────────┬──────────┬──────────┬──────────┐         │  │
│  │ │   지표   │  자사    │ 업종평균 │ 차이     │         │  │
│  │ ├──────────┼──────────┼──────────┼──────────┤         │  │
│  │ │ CTR      │  1.2%   │  1.8%   │ ↓ 33%   │         │  │
│  │ │ CPC      │ ₩1,200  │ ₩980    │ ↑ 22%   │         │  │
│  │ │ 전환율    │  3.5%   │  4.2%   │ ↓ 17%   │         │  │
│  │ │ ROAS     │  2.8    │  3.4    │ ↓ 18%   │         │  │
│  │ └──────────┴──────────┴──────────┴──────────┘         │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                               │
│  🔍 성과가 낮은 이유 분석                                     │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ AI 분석 결과:                                          │  │
│  │                                                         │  │
│  │ 1. 색상 선택 문제                                      │  │
│  │    • 자사: 빨강+검정 (긴장감 과다)                     │  │
│  │    • 업종 평균: 파랑+주황 (신뢰+활력)                  │  │
│  │    → 권장: 색상 팔레트 변경                           │  │
│  │                                                         │  │
│  │ 2. 카피 길이 문제                                      │  │
│  │    • 자사 헤드라인: 평균 28자 (너무 김)                │  │
│  │    • 업종 평균: 17자                                   │  │
│  │    → 권장: 핵심 메시지만 남기고 간결화                │  │
│  │                                                         │  │
│  │ 3. 레이아웃 문제                                       │  │
│  │    • 자사: 전면 텍스트형 (70%)                         │  │
│  │    • 업종 평균: 상하분할형 (60%)                       │  │
│  │    → 권장: 이미지 비중 증가                           │  │
│  │                                                         │  │
│  │ [개선 소재 자동 생성하기]                              │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘

광고별 상세 비교
┌─────────────────────────────────────────────────────────────┐
│  자사 광고 상세                                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ 광고명: 3월 신학기 특별반 모집                         │  │
│  │ 게재기간: 15일                                         │  │
│  │ CTR: 1.1% (업종평균 1.8% 대비 -39%)                    │  │
│  │                                                         │  │
│  │ [자사 광고 이미지]  vs  [유사 경쟁사 광고]              │  │
│  │                                                         │  │
│  │ 차이점:                                                 │  │
│  │ • 색상: 빨강+검정 vs 파랑+주황                         │  │
│  │ • 레이아웃: 전면텍스트 vs 상하분할                     │  │
│  │ • 카피 길이: 28자 vs 17자                              │  │
│  │ • 인물: 없음 vs 있음                                   │  │
│  │                                                         │  │
│  │ [이 광고 개선안 생성하기]                              │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

#### 2.3 A/B 테스트 추적

**기능:**
- 생성한 소재로 실제 광고 집행 시 성과 추적
- 버전별 성과 비교
- 승리 패턴 학습 및 피드백

**데이터 스키마:**
```sql
CREATE TABLE ab_tests (
    id SERIAL PRIMARY KEY,
    test_name VARCHAR(255),
    hypothesis TEXT,
    start_date DATE,
    end_date DATE,
    status VARCHAR(50),
    created_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE ab_test_variants (
    id SERIAL PRIMARY KEY,
    test_id INTEGER REFERENCES ab_tests(id),
    variant_name VARCHAR(50), -- 'A', 'B', 'C'
    generated_creative_id INTEGER, -- 생성된 소재 ID
    ad_id VARCHAR(255) REFERENCES own_ads(ad_id),
    traffic_allocation DECIMAL(5,2), -- 33.33%
    created_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE ab_test_results (
    id SERIAL PRIMARY KEY,
    test_id INTEGER REFERENCES ab_tests(id),
    variant_id INTEGER REFERENCES ab_test_variants(id),
    date DATE,
    impressions INTEGER,
    clicks INTEGER,
    conversions INTEGER,
    spend DECIMAL(10,2),
    calculated_at TIMESTAMP DEFAULT NOW(),
    UNIQUE(variant_id, date)
);
```

---

### Phase 4: 자동화 및 고도화

#### 4.1 정기 리포트 자동 생성

**기능:**
- 주간/월간 트렌드 리포트 자동 생성
- 신규 베스트 프랙티스 알림
- 성과 하락 광고 자동 감지 및 개선안 제시

#### 4.2 업종 확장

**지원 업종:**
- ✅ 입시학원 (MVP)
- 의료/뷰티 (다이어트, 성형, 피부과, 모발이식)
- 어학원
- 직업/자격증 학원
- 예체능 학원
- 기타 (커스텀)

#### 4.3 고급 기능

**예정 기능:**
- 동영상 광고 분석 및 생성
- 다국어 지원 (영어, 중국어)
- 랜딩 페이지 분석 및 추천
- 타겟팅 최적화 제안
- 예산 배분 시뮬레이션

---

## 📅 개발 로드맵

### MVP Phase (Week 1-8)

**Week 1-2: 인프라 구축**
- [ ] 프로젝트 셋업 (레포지토리, 환경 설정)
- [ ] Docker 개발 환경 구축
- [ ] PostgreSQL + Redis 셋업
- [ ] FastAPI 기본 구조
- [ ] Next.js 프로젝트 초기화

**Week 3-4: Phase 1 - 데이터 수집**
- [ ] Meta Ad Library API 연동
- [ ] 광고 수집 스크립트 개발
- [ ] 이미지 다운로드 및 S3 저장
- [ ] 데이터베이스 스키마 구현
- [ ] 수집 API 엔드포인트 구현

**Week 5-6: Phase 1 - 분석 엔진**
- [ ] Claude Vision API 연동
- [ ] 이미지 분석 파이프라인
- [ ] GPT-4 카피 분석 파이프라인
- [ ] 분석 결과 저장 및 조회 API

**Week 7-8: Phase 1 - 대시보드**
- [ ] 광고 갤러리 UI
- [ ] 트렌드 분석 차트
- [ ] 베스트 프랙티스 페이지
- [ ] 필터링 및 검색 기능

### Beta Phase (Week 9-16)

**Week 9-10: Phase 3 - 인사이트 엔진**
- [ ] 패턴 분석 로직 구현
- [ ] 인사이트 생성 및 캐싱
- [ ] 통계적 유의성 검증

**Week 11-12: Phase 3 - 소재 생성**
- [ ] 전략 생성 로직
- [ ] 카피 생성 (GPT-4)
- [ ] "선택 이유" 생성 (Claude)
- [ ] 목업 렌더링 (HTML/CSS)

**Week 13-14: Phase 3 - 생성 UI**
- [ ] 소재 생성 입력 폼
- [ ] 3버전 결과 화면
- [ ] 편집 기능
- [ ] 다운로드 기능

**Week 15-16: Phase 2 - 성과 연동**
- [ ] Meta Marketing API 연동
- [ ] 자사 광고 데이터 수집
- [ ] 성과 비교 대시보드
- [ ] A/B 테스트 추적

### Launch Phase (Week 17-20)

**Week 17-18: 최적화 및 테스트**
- [ ] 성능 최적화
- [ ] 버그 수정
- [ ] 사용성 테스트
- [ ] 문서화

**Week 19-20: 배포 및 런칭**
- [ ] 프로덕션 환경 구축
- [ ] CI/CD 파이프라인
- [ ] 모니터링 설정
- [ ] 베타 사용자 온보딩

---

## 💰 비즈니스 모델

### 가격 정책 (SaaS)

**Starter Plan - ₩99,000/월**
- 광고 수집: 월 500개
- AI 분석: 월 100개
- 소재 생성: 월 20개
- 업종: 1개 선택
- 사용자: 1명

**Professional Plan - ₩299,000/월** ⭐ 추천
- 광고 수집: 월 2,000개
- AI 분석: 월 500개
- 소재 생성: 월 100개
- 업종: 3개 선택
- 사용자: 3명
- 자사 광고 연동 (1개 계정)
- 성과 비교 대시보드
- 주간 리포트

**Agency Plan - ₩899,000/월**
- 광고 수집: 무제한
- AI 분석: 무제한
- 소재 생성: 월 500개
- 업종: 전체
- 사용자: 10명
- 자사 광고 연동 (5개 계정)
- 화이트라벨 지원
- API 액세스
- 전담 CS

### 추가 수익원

**컨설팅 서비스**
- 광고 전략 수립: ₩3,000,000~
- 크리에이티브 워크샵: ₩2,000,000~
- 정기 분석 리포트: ₩500,000/월~

**데이터 판매**
- 업종별 트렌드 리포트: ₩500,000~
- 커스텀 리서치: ₩2,000,000~

---

## 📊 성공 지표 (KPI)

### 제품 지표
- MAU (Monthly Active Users): 목표 50명 (6개월)
- 구독 유지율: 목표 85%+
- 소재 생성 완료율: 목표 70%+
- 사용자당 월 평균 소재 생성 수: 목표 15개

### 비즈니스 지표
- MRR (Monthly Recurring Revenue): 목표 ₩5,000,000 (12개월)
- CAC (Customer Acquisition Cost): 목표 ₩300,000 이하
- LTV (Lifetime Value): 목표 ₩3,600,000+
- LTV/CAC 비율: 목표 12:1

### 제품 품질 지표
- AI 분석 정확도: 목표 90%+ (사람 평가 기준)
- 소재 생성 만족도: 목표 4.5/5.0
- 시스템 응답 시간: 목표 < 3초 (소재 생성)
- API 가용성: 목표 99.9%

---

## 🎯 차별화 포인트

### 경쟁사 대비 강점

**vs. 수동 벤치마킹**
- ✅ 자동화로 시간 70% 절감
- ✅ 데이터 기반 의사결정
- ✅ 놓치는 인사이트 없음

**vs. 일반 Ad Spy 툴 (AdSpy, BigSpy 등)**
- ✅ 단순 수집이 아닌 AI 분석 및 추천
- ✅ 한국 시장 특화 (지역, 업종)
- ✅ 소재 생성까지 원스톱

**vs. 디자인 툴 (Canva, Figma 등)**
- ✅ 템플릿이 아닌 데이터 기반 맞춤 생성
- ✅ "왜"를 설명하는 인사이트 제공
- ✅ 성과 추적 및 학습

---

## 🚀 Go-to-Market 전략

### Phase 1: 내부 검증 (Month 1-2)
- Glitzy 내부에서 실제 클라이언트에 적용
- 피드백 수집 및 개선
- 성과 측정 (광고 CTR, 제작 시간 등)

### Phase 2: 베타 런칭 (Month 3-4)
- 지인 에이전시 5-10곳 무료 베타 테스트
- 사용 사례(Case Study) 축적
- 제품 개선

### Phase 3: 정식 런칭 (Month 5-6)
- 디지털 마케팅 커뮤니티 공략
- 웨비나 개최 (주제: "AI로 광고 성과 2배 높이기")
- 무료 트라이얼 제공

### Phase 4: 확장 (Month 7-12)
- 콘텐츠 마케팅 (블로그, 유튜브)
- 파트너십 (마케팅 교육 기관, 협회)
- 해외 진출 준비 (영문 버전)

---

## 🔐 보안 및 컴플라이언스

### 데이터 보안
- Meta API 토큰 암호화 저장
- 이미지 파일 S3 Private 설정
- 사용자 데이터 HTTPS 전송
- 정기적 백업 및 복구 테스트

### 개인정보 보호
- 광고 데이터는 공개된 정보만 수집
- 사용자 개인정보 최소 수집
- GDPR 준수 (EU 확장 시)

### API 사용 규정 준수
- Meta API 이용 약관 준수
- 수집 데이터 사용 목적 명시
- Rate Limit 관리

---

## 📝 향후 발전 방향

### 단기 (6개월)
- [ ] 입시학원 외 의료/뷰티 업종 추가
- [ ] 동영상 광고 분석 지원
- [ ] 모바일 앱 출시

### 중기 (12개월)
- [ ] 타 플랫폼 지원 (Google Ads, Naver)
- [ ] 랜딩 페이지 분석 및 최적화
- [ ] AI 기반 타겟팅 추천

### 장기 (24개월)
- [ ] 글로벌 시장 진출
- [ ] 자체 크리에이티브 생성 AI 개발
- [ ] 마케팅 자동화 플랫폼으로 확장

---

## 📞 Contact & Support

**개발팀**
- Product Lead: [담당자]
- Tech Lead: [담당자]
- Designer: [담당자]

**외부 협력**
- AI Advisor: Anthropic (Claude), OpenAI (GPT-4)
- Cloud Provider: AWS/GCP
- Legal Advisor: [법무 자문]

---

## 부록

### A. API 명세서
별도 문서 참조: `API_SPECIFICATION.md`

### B. 데이터베이스 스키마
별도 문서 참조: `DATABASE_SCHEMA.md`

### C. 개발 가이드
별도 문서 참조: `DEVELOPMENT_GUIDE.md`

### D. 배포 가이드
별도 문서 참조: `DEPLOYMENT_GUIDE.md`

---

**문서 버전**: 1.0  
**최종 수정일**: 2026-02-09  
**작성자**: Meta Ad Analyzer 기획팀  
**승인자**: [승인자명]

---

